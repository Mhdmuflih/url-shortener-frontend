
const Button = () => {
    return (
        <div>
            <button className={`bg-green-600 text-wrap font-bold items-center text-center`}>{}</button>
        </div>
    )
}

export default Button;
